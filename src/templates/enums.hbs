
/**
 * * notice this file create by enum-maker
 * * https://github.com/diveDylan/enum-maker
 * @description 系统字典枚举
 */
{{#each enums}}
/**
 * @description {{name}}
 */
export enum {{name}} {
  {{#each enums}}
  /**
  * @description {{label}}
  */
  {{value}} = '{{value}}',
  {{/each}}
}
{{/each}}
